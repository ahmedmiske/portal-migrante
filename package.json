{
  "private": true,
  "scripts": {
    "build": "cd portal-migrante/backend && npm install && npm run build",
    "start": "cd portal-migrante/backend && npm start",
    "typecheck": "cd portal-migrante/backend && npm run typecheck"
  }
}
